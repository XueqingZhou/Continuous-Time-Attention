# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Python tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
ENV/

# Build / packaging
build/
dist/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/

# Large generated artifacts (reproducible via scripts)
results/
profiling/out/

# Traces and profiler exports
**/trace_*.json
**/summary_*.txt

# Model weights / large checkpoints
# - If you want to version them, use Git LFS (see `.gitattributes`).
# - If you don't want to version them, simply avoid adding them to git.

# Local notes / planning docs (do not commit)
local/

